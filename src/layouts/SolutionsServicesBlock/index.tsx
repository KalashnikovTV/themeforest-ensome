import SolutionsCardList from '@components/SolutionsCardList';
import { Container } from '@theme/theme';

const SolutionsServicesBlock: React.FC = () => {
  return (
    <Container>
      <SolutionsCardList />
    </Container>
  );
};

export default SolutionsServicesBlock;
